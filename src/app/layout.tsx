import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ZenRider - Live Intentionally, Fueled by Health, Motion & Mindfulness',
  description: 'ZenRider is a personal dashboard of movement, creativity, and purpose — blending fitness stats, photography, life experience, and insights into one living, breathing digital story.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <main className="min-h-screen bg-white">
          {children}
        </main>
      </body>
    </html>
  )
} 